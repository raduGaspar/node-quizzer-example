# node-quizzer-example

- clone repo
- run `npm install -d` to install npm dependencies
- run `node app.js` to start the server
- enjoy

[node-quizzer|https://www.npmjs.com/package/node-quizzer]